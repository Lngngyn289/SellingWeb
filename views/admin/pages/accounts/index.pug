extends ../../layouts/default.pug

block main
  h1 Products Category List 

  .card.mb-3 
    .card-header Accounts List
    .card-body  
      .row 
        .col-8 
        .col-4.text-right
          a(
            href=`${prefixAdmin}/accounts/create`
            class="btn btn-success"
          ) Create Account

      div #{records}